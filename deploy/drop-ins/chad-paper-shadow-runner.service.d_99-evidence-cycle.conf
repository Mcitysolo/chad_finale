[Service]
# Evidence pack after each shadow tick (bounded + auditable). Never fail unit on degraded evidence.
ExecStartPost=/bin/bash -lc '/usr/bin/timeout 120s /usr/bin/sudo -n /usr/bin/python3 /usr/local/bin/chad_evidence_cycle.py --duration-min 1 || true'
ExecStartPost=/usr/local/bin/chad_evidence_post_marker.sh
