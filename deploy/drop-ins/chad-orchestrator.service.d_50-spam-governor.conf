[Service]
# Phase 11 Edge Quality: prevent Beta (or any brain) from flooding the system.
# Safe: only filters signal output; does not enable live trading.
Environment=CHAD_SPAM_GOVERNOR_ENABLED=1
Environment=CHAD_SPAM_MAX_PER_STRATEGY_PER_CYCLE=6
Environment=CHAD_SPAM_MAX_PER_STRATEGY_SYMBOL_PER_CYCLE=2
